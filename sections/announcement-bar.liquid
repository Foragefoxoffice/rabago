<section 
  class="announcement-bar flex text-white py-2 overflow-hidden" 
  style="background-color: {{ section.settings.bg_color }}; color: {{ section.settings.text_color }};">

  <div class="marquee flex whitespace-nowrap gap-16">
    {% for block in section.blocks %}
      <span class="mx-8">{{ block.settings.text }}</span>
    {% endfor %}
    {%- comment -%} Duplicate announcements for seamless scroll {%- endcomment -%}
    {% for block in section.blocks %}
      <span class="mx-8">{{ block.settings.text }}</span>
    {% endfor %}
  </div>

  <style>
    .marquee {
      display: inline-flex;
      min-width: max-content;
      animation: scroll-left 20s linear infinite;
    }
    @keyframes scroll-left {
      0% { transform: translateX(100%); }
      100% { transform: translateX(-100%); }
    }
  </style>
</section>

{% schema %}
{
  "name": "Announcement Bar",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "announcement",
      "name": "Announcement",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Message",
          "default": "Announce something here"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Scrolling Announcement Bar",
      "category": "Text",
      "blocks": [
        { "type": "announcement", "settings": { "text": "Free shipping on all orders over ₹999!" } },
        { "type": "announcement", "settings": { "text": "New arrivals are here 🚀" } },
        { "type": "announcement", "settings": { "text": "Limited time offer – 20% off" } },
        { "type": "announcement", "settings": { "text": "Sign up & get exclusive deals 🎉" } }
      ]
    }
  ]
}
{% endschema %}
